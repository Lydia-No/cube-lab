[project]
name = "symbolic-engine"
version = "0.1.0"
requires-python = ">=3.10"

dependencies = [
    "symbolic-core"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["symbolic_engine"]
